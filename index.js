/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';

import HooksApp from './example/HooksApp';
AppRegistry.registerComponent(appName, () => HooksApp);

// import DrawableMap from './example/DrawableMap';
// AppRegistry.registerComponent(appName, () => DrawableMap);
